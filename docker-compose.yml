version: '3.8'

services:
  frontend:
    build: .
    container_name: coabi-app
    ports:
      - "8081:8081"
    environment:
      - EXPO_PUBLIC_API_BASE_URL=${EXPO_PUBLIC_API_BASE_URL}
    restart: unless-stopped
